name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches:
      - "**" 

jobs:
  ai-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      issues: write
      pull-requests: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Get changed files
        id: changed-files
        uses: tj-actions/changed-files@v44
        with:
          files: |
            **/*.js
            **/*.jsx
            **/*.ts
            **/*.tsx
            **/*.py
            **/*.java
            **/*.cpp
            **/*.c
            **/*.cs
            **/*.go
            **/*.rs
            **/*.php
            **/*.rb
            **/*.yml
            **/*.yaml
          separator: ' '

      - name: Debug Context
        run: |
          echo "Event: ${{ github.event_name }}"
          echo "Repository: ${{ github.repository }}"
          echo "Actor: ${{ github.actor }}"
          echo "SHA: ${{ github.sha }}"
          echo "Ref: ${{ github.ref }}"

      - name: Run AI Code Review
        if: steps.changed-files.outputs.any_changed == 'true'
        run: |
          node --input-type=module - <<'EOF'
          import fs from 'fs';
          import path from 'path';
          import { Octokit } from '@octokit/rest';
          import axios from 'axios';

          const octokit = new Octokit({ auth: process.env.GITHUB_TOKEN });
          const changedFiles = process.env.CHANGED_FILES?.split(' ') || [];
          const context = JSON.parse(process.env.GITHUB_CONTEXT || '{}');

          console.log('=== DEBUG: Context Analysis ===');
          console.log('Event name:', context.event_name);
          console.log('Repository:', context.repository);
          console.log('Repository full_name:', context.repository?.full_name);
          console.log('Repository name:', context.repository?.name);
          console.log('Repository owner:', context.repository?.owner?.login);
          console.log('Actor:', context.actor);
          console.log('Ref:', context.ref);
          console.log('SHA:', context.sha);
          console.log('================================');

          const getLanguage = (ext) => ({
            '.js': 'javascript',
            '.ts': 'typescript',
            '.jsx': 'javascriptreact',
            '.tsx': 'typescriptreact',
            '.py': 'python',
            '.java': 'java',
            '.cpp': 'cpp',
            '.c': 'c',
            '.cs': 'csharp',
            '.go': 'go',
            '.rs': 'rust',
            '.php': 'php',
            '.rb': 'ruby',
            '.yml': 'yaml',
            '.yaml': 'yaml'
          }[ext] || 'text');

          async function analyzeCode(filePath, code) {
            const prompt = `
            You are a senior software engineer reviewing code. 
            Provide a JSON response:

            {
              "score": 1-10,
              "summary": "overall quality summary",
              "comments": [
                {
                  "line": number | null,
                  "type": "bug|performance|style|security|suggestion",
                  "severity": "low|medium|high",
                  "message": "Detailed feedback with reasoning and improvement suggestions (no fenced code blocks, describe changes in words or pseudo-code)."
                }
              ]
            }

            Code:
            ---
            ${code}
            ---
            `;

            try {
              const res = await axios.post(
                "https://api.groq.com/openai/v1/chat/completions",
                {
                  model: "llama3-8b-8192",
                  messages: [{ role: "user", content: prompt }],
                  temperature: 0.3
                },
                {
                  headers: {
                    Authorization: \`Bearer \${process.env.GROQ_API_KEY}\`,
                    "Content-Type": "application/json"
                  }
                }
              );

              const content = res.data.choices[0].message.content || "";
              const match = content.match(/\{[\s\S]*\}/);

              if (match) {
                try {
                  return JSON.parse(match[0]);
                } catch {
                  console.warn("⚠️ Invalid JSON from AI, fallback to summary text.");
                  return { score: 5, summary: content, comments: [] };
                }
              }
              return { score: 5, summary: content, comments: [] };
            } catch (e) {
              console.error("❌ Error analyzing file:", filePath, e.message);
              return { score: 0, summary: "AI analysis failed", comments: [] };
            }
          }

          async function postPRComments(reviews) {
            const pullRequest = context.pull_request;
            if (!pullRequest) {
              console.error("❌ No pull request context found");
              return;
            }

            let owner, repo;
            if (context.repository?.full_name) {
              [owner, repo] = context.repository.full_name.split('/');
            } else if (context.repository?.owner?.login && context.repository?.name) {
              owner = context.repository.owner.login;
              repo = context.repository.name;
            } else {
              console.error("❌ Cannot determine repository owner/name");
              return;
            }

            const prNumber = pullRequest.number;

            for (const review of reviews) {
              if (!review || !review.comments) continue;

              for (const comment of review.comments) {
                try {
                  await octokit.pulls.createReviewComment({
                    owner,
                    repo,
                    pull_number: prNumber,
                    commit_id: pullRequest.head.sha,
                    path: review.filePath,
                    body: comment.message,
                    line: comment.line || 1,
                    side: 'RIGHT'
                  });
                  console.log(`✅ Posted comment for ${review.filePath}:${comment.line || 1}`);
                } catch (error) {
                  console.error(`❌ Failed to post comment for ${review.filePath}:`, error.message);
                }
              }
            }
          }

          async function postPushIssue(reviews) {
            let owner, repo;
            if (context.repository?.full_name) {
              [owner, repo] = context.repository.full_name.split('/');
            } else if (context.repository?.owner?.login && context.repository?.name) {
              owner = context.repository.owner.login;
              repo = context.repository.name;
            } else if (context.repositoryUrl) {
              const match = context.repositoryUrl.match(/github\.com\/([^\/]+)\/([^\/]+)\.git/);
              if (match) {
                owner = match[1];
                repo = match[2];
              }
            }

            if (!owner || !repo) {
              console.error("❌ Cannot determine repository owner/name");
              return;
            }

            const commitSha = context.sha?.substring(0, 7) || "unknown";
            let body = `## 🤖 AI Code Review - Commit \`${commitSha}\`\n\n`;
            body += `**Event:** ${context.event_name}\n`;
            body += `**Branch:** ${context.ref?.replace('refs/heads/', '') || 'unknown'}\n`;
            body += `**Author:** ${context.actor || 'unknown'}\n\n`;

            for (const r of reviews) {
              if (!r) continue;
              body += `### 📄 \`${r.filePath}\`\n`;
              body += `**Score:** ${r.score}/10 ⭐\n`;
              body += `**Summary:** ${r.summary}\n\n`;

              if (r.comments?.length > 0) {
                body += `**Issues Found:**\n`;
                r.comments.forEach(c => {
                  const emoji = c.severity === 'high' ? '🚨' : c.severity === 'medium' ? '⚠️' : '💡';
                  body += `${emoji} **${c.type?.toUpperCase()}** (${c.severity}): ${c.message}\n`;
                });
              } else {
                body += `✅ **No issues found!**\n`;
              }
              body += `\n---\n`;
            }

            try {
              const issue = await octokit.issues.create({
                owner,
                repo,
                title: `🤖 AI Code Review - ${commitSha}`,
                body,
                labels: ['ai-review', 'code-quality']
              });
              console.log(`✅ Created issue #${issue.data.number} for commit ${commitSha}`);
            } catch (error) {
              console.error("❌ Error creating issue:", error.message);
            }
          }

          (async () => {
            console.log(`🚀 Starting AI Code Review...`);
            console.log(`📝 Event: ${context.event_name}`);
            console.log(`📁 Changed files: ${changedFiles.join(', ')}`);

            if (changedFiles.length === 0) {
              console.log("⚠️  No changed files found, exiting...");
              return;
            }

            let reviews = [];
            for (const filePath of changedFiles) {
              if (!fs.existsSync(filePath)) {
                console.log(`⚠️  Skipping deleted file: ${filePath}`);
                continue;
              }

              console.log(`🔍 Analyzing: ${filePath}`);
              const code = fs.readFileSync(filePath, 'utf8');

              if (code.length > 50000) {
                console.log(`⚠️  Skipping large file (${code.length} chars): ${filePath}`);
                continue;
              }

              const result = await analyzeCode(filePath, code);
              if (result) {
                result.filePath = filePath;
                reviews.push(result);
                console.log(`✅ Analysis complete for ${filePath}: Score ${result.score}/10`);
              }
            }

            if (reviews.length === 0) {
              console.log("⚠️  No files were analyzed, exiting...");
              return;
            }

            if (context.event_name === 'pull_request') {
              console.log("🔄 Processing as pull request...");
              await postPRComments(reviews);
            } else {
              console.log("🔄 Processing as push event...");
              await postPushIssue(reviews);
            }

            console.log("🎉 AI Code Review completed!");
          })();
          EOF
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}
          CHANGED_FILES: ${{ steps.changed-files.outputs.all_changed_files }}
          GITHUB_CONTEXT: ${{ toJson(github) }}